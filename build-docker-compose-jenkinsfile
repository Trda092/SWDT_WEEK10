pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo Build in progress.'
                sh 'systemctl status docker'
                sh 'docker-compose up'
            }
    }
}
}
