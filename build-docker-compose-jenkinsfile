pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                sh 'echo Build in progress.'
                sh 'sudo groupadd docker'
                sh 'sudo usermod -aG docker ec2-user'
                sh 'sudo newgrp docker'
                dir('backend-site'){
                sh 'ls'
                sh 'docker build -t backend:v1 .'
                sh 'docker run -d -p 8088:80 backend:v1'
            }
            }
    }
}
}
